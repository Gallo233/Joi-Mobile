<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- A platform theme, deliberately: Compose draws everything above it, and a
         Material Components XML theme would add a dependency for a window
         background. -->
    <style name="Theme.Joi" parent="@android:style/Theme.Material.NoActionBar" />
</resources>
